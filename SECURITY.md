# Security

Do not report vulnerabilities in public issues. Use GitHub's private security
advisory workflow for this repository.

The plugin never accepts database credentials or provisions engines. Treat
scope labels, subject identifiers, dimension values, correlation metadata,
and provenance as potentially sensitive. Do not place secrets in them.
